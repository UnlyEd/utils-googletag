const {
  pageview,
  generateTracker,
  gaPageview,
  gtagEvent,
  initializeAsHTML,
} = require('./lib/googleTag');

module.exports = {
  pageview,
  generateTracker,
  gaPageview,
  gtagEvent,
  initializeAsHTML,
};
